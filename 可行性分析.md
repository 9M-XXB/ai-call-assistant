# 可行性分析：安卓 AI 来电助手（2026-09-12）

## 0. TL;DR

- **市面上有直接同类产品**：Google Pixel Call Screen、三星 Bixby Text Call、小米「小爱通话」、Truecaller Assistant。前三个是**手机厂商系统级实现**；Truecaller 是唯一接近"第三方 App"的成功案例，而它在美國的实现方式是**把来电呼叫转移到其云端 AI 接听**（靠运营商呼叫转移功能，不是运营商语音信箱）。
- 核心结论一句话：**"自动接听 + 录音 + AI 摘要"作为第三方 App 可行；"把自定义语音播给对方听"是唯一撞墙的需求——Android 无公开 API 向通话音频流注入声音**。成熟产品全部用两条路绕过：①厂商系统级权限；②云端接线（呼叫转移）。这也是本项目的两条现实路线。
- 纯端侧的"免提外放"方案（App 自动接听后开免提、外放语音、麦克风录音）在技术上能跑通，适合 MVP 和个人使用，但音质和体验做不出商业产品。

---

## 1. 需求逐条可行性判定

| # | 需求 | 判定 | 说明 |
|---|---|---|---|
| 1 | 10 秒未接自动接听 | ✅ 可行 | `TelecomManager.acceptRingingCall()`（需 `ANSWER_PHONE_CALLS` 运行时权限，API 26+，**API 29 已弃用**但多数机型仍可用）；正规做法是实现 `InCallService` 并申请**默认拨号器角色（ROLE_DIALER）**，在 `onCallAdded` 后计时 10 秒调 `call.answer()` |
| 2 | 播放自定义语音给对方 | ⚠️ **无公开 API** | 通话音频流由基带处理，第三方 App 无法注入。绕法 A：开免提外放让麦克风拾音（质量差）；绕法 B：云端接线（呼叫转移，由云侧 TTS 播放）；绕法 C：系统级/厂商合作 |
| 3 | 录对方留言/对话 | ⚠️ 受限 | `AudioSource.VOICE_CALL`（双向）需 `CAPTURE_AUDIO_OUTPUT`（**signature\|privileged，第三方拿不到**）；`VOICE_DOWNLINK/UPLINK` 多数机型无效。可用绕法：免提+麦克风（`MIC` 或 `VOICE_COMMUNICATION`）录房间声音；或云端接线由云侧录音 |
| 4 | 转写 + AI 对话摘要 | ✅ 成熟 | Whisper（本地/云）转写 + LLM 生成摘要，无技术风险 |
| 5 | 多语言自定义语音 | ✅ 成熟 | 预录音频直接播放；AI 生成可用 TTS/声音克隆（如 Microsoft Azure Personal Voice、ElevenLabs），按语言配置多套版本 |
| 6 | 不依赖运营商语音信箱 | ✅ 可满足 | 所有方案都不碰运营商语音信箱；但**云端接线方案会用到运营商的"呼叫转移"功能**（设置 `*61` 类无应答转移码），这是否越界需要你决策（见第 6 节） |

---

## 2. Android 能力边界（官方文档核实，2026-09-12）

**自动接听**
- `TelecomManager.acceptRingingCall()`："accepts the call on behalf of the user"，要求 `ANSWER_PHONE_CALLS`（第三方可申请的运行时权限）或 `MODIFY_PHONE_STATE`（系统级）；**API 29 起官方弃用**，建议改用 `InCallService`。实测层面多数机型仍可用，但非长期保证。
- 正规路线：App 实现 `InCallService`，通过 `RoleManager.createRequestRoleIntent(ROLE_DIALER)` 请用户授权成为**默认拨号器**；之后 `onCallAdded(Call)` 里可 `call.answer()` / `call.disconnect()`，可 `setMuted()`、`setAudioRoute(SPEAKER)`（API 34 起改为 `requestCallEndpointChange`）——**可编程开免提**。代价：接管整个来电 UI（需自己画接听界面、响铃，声明 `METADATA_IN_CALL_SERVICE_RINGING`）。
- `CallScreeningService` 可在响铃前看到来电号码并允许/拒接，适合做"白名单才启用"的策略，但接听仍靠上面两条。

**通话音频（全案的关键约束）**
- 录音：`MediaRecorder.AudioSource.VOICE_CALL`（双向）官方文档明确 **需要 `CAPTURE_AUDIO_OUTPUT`**，该权限为 signature|privileged，第三方 App 无法获得；`VOICE_DOWNLINK`/`VOICE_UPLINK` 在很多设备上直接返回静音。Android 10 起对非通话音源的采集也收紧（`AudioPlaybackCapture` 明确排除通话）。
- 播放：**不存在任何公开 API 把 App 音频送入通话上下行**。`InCallService` 的音频 API 只能切换路由（扬声器/听筒/蓝牙），不能注入内容。
- 推论：第三方 App 想让对方听到自定义语音，物理上只剩"扬声器外放 → 环境麦克风拾音"这一条路；想录对方声音，同样只剩麦克风。免提方案的所有细节由此展开。

**免提方案的工程细节**
- 路由：默认拨号器角色下 `setAudioRoute(SPEAKER)` 编程开免提；无角色时可用 `AudioManager.setSpeakerphoneOn(true)`（部分系统版本需要 `MODIFY_AUDIO_SETTINGS`，且行为碎片化）。
- 录音源选择：用 `MIC`（原始麦克风）而非 `VOICE_COMMUNICATION`——后者的回声消除（AEC）会把扬声器放出来的声音从麦克风信号里减掉，**恰好把对方经扬声器传回的声音也消掉了**；用 `MIC` 则双方声音都录到（含自己机器人的声音，可后处理降噪）。
- 已知缺陷：外放音质差（对方听到的是"电话里的扬声器声"）、环境噪声会进录音、外放内容旁人可听见、部分机型通话中麦克风路径被系统独占导致拾音异常（碎片化需真机矩阵测试）。

---

## 3. 四种实现架构对比

| 架构 | 思路 | 语音播放 | 录音 | 音质/体验 | 成本/门槛 |
|---|---|---|---|---|---|
| **A. 纯端侧 App**（免提绕过） | 自动接听 + 免提外放 + 麦克风录音 + 本地/云 ASR + LLM 摘要 | 外放，对方能听到但音质差 | 麦克风（双方混音） | 差，环境敏感 | 低；只有普通权限，可上架（功能受限） |
| **B. 云端接线**（Truecaller/YouMail 路线） | 无应答呼叫转移到云号码；云侧 IVR 播 TTS、录音、ASR、LLM；App 推送摘要 | 云侧 TTS/克隆语音，**音质完美** | 云侧录音，**完美** | 好 | 语音云服务按分钟计费；**依赖运营商呼叫转移功能**；配置对用户有门槛（拨转移码） |
| **C. 系统级/厂商合作**（Pixel/三星/小米路线） | 成为系统应用或与 OEM 集成，拿特权权限（`CAPTURE_AUDIO_OUTPUT`、通话注入） | 通话流内注入，原生体验 | 通话流内录音 | 最好 | 门槛极高：需 OEM 合作、刷机适配或自研 ROM |
| **D. 自建 VoIP 通道** | 给用户发一个虚拟号/App 内通话，走自建 SIP/WebRTC | 完全掌控 | 完全掌控 | 好 | 对方必须拨你的号 → 改变对方行为，场景不成立（对方拨的是你的手机号） |

> D 补充：若给 App 配 PSTN 虚拟号码并做入呼聚合，就退化成架构 B 的自建版（还是要转移/换号），本质相同。

**结论**：商业级体验只有 B（和 C）；A 是唯一"一个独立 APK 就交付"的方案。Truecaller 在美国选择 B 的事实本身就是对 A 天花板的市场验证。

---

## 4. 竞品与实现方式（2026-09-12 调研）

| 产品 | 形态 | 能力 | 实现方式 | 多语言/自定义语音 |
|---|---|---|---|---|
| **Google Pixel Call Screen / Call Assist** | Pixel 系统电话应用 | 自动/手动代接、实时转写、AI 应答、垃圾电话过滤 | 系统级：Google Dialer 拥有特权通话音频通路（非 Pixel 不可用） | 按市场逐步扩展语言（美/英/日等），不可自定义音色 |
| **三星 Bixby Text Call（Call Assist 套件）** | One UI 系统功能 | 代接、你打字→TTS 念给对方、对方语音→实时转写、快捷回复 | 系统级：One UI 与通话栈深度集成 | 韩语→英语→法/德/意/西、印度市场等；**一次只激活一种语言**（双卡可按 SIM 配语言）；可选系统音色 |
| **小米「小爱通话」（原 AI 通话）** | MIUI 12+ 系统功能 | 自动接听（可设规则，如未接 N 秒/防骚扰）、AI 代接代拨、实时字幕、男女声选择 | 系统级：MIUI 电话栈内置；隐私政策显示需要通话权限，Android 9 为权限分界 | 中文为主；可选男女声，非声音克隆 |
| **Truecaller Assistant** | 第三方 App（Android+iOS，Premium 订阅） | 自动代接、真人感 AI 对话、实时转写、留言、录音（地区允许处）、垃圾过滤 | **美国等市场：按运营商指引设置呼叫转移，把来电转到 Truecaller 云端 AI 接听**；App 只做控制面板和结果展示 | 预置音色；2024 年起接 **Microsoft Azure "Personal Voice"**，可克隆用户本人声音——与你"自定义预录/AI 生成语音"的设想几乎一致 |
| **YouMail / Google Voice** | 第三方 App + 云服务 | 视觉语音信箱替代、留言转写 | 运营商**条件呼叫转移**（无应答/无信号转移码）→ 云端接听留言 → 转写 | 属于"语音信箱替代品"，功能是你需求的前半段（无 AI 对话） |

**共同规律**：凡是"声音能高质量到达对方"的产品，要么是系统级（前三），要么把接听点放到网络云侧（Truecaller/YouMail）。**没有任何一个第三方 App 是靠端侧注入音频实现顶级体验的**——因为 OS 根本不开放。

---

## 5. 端侧 MVP 的功能蓝图（架构 A，1–2 人月可验证）

1. **来电监控与自动接听**：`InCallService`（申请 ROLE_DIALER）或 `acceptRingingCall`；`onCallAdded` 起计时 10s；期间用户自己接了则取消。
2. **播放留言提示音**：`setAudioRoute(SPEAKER)` → `MediaPlayer` 播放用户选定的语言版本音频（预录文件或 TTS 预生成文件；AI 克隆声音可离线生成后当预录用，App 内不强制实时 TTS）。
3. **录音**：`MediaRecorder` + `AudioSource.MIC`（免提开着，对方声音经扬声器进入麦克风）；录音前弹本地通知提示"正在录音"（合规）。
4. **转写与摘要**：Whisper（本地 `whisper.cpp` 或云 API）→ 说话人粗分（自己的机器人语音已知文本，可对齐剔除）→ LLM 生成摘要（任务、来电人诉求、是否需回电）→ 通知栏推送。
5. **多语言管理**：按语言/场景组织音频库（中文-工作、英文-工作……），来电时可按 SIM 卡/联系人归属自动选语言（对标三星"按 SIM 配语言"）。

已知体验上限：对方听到的语音带"扬声器电声感"；嘈杂环境下对方声音可能录不清；手机外放留言内容旁人可闻。

---

## 6. 决策点与开放问题

- **"呼叫转移"是否违反你的约束？** 你排除了"运营商语音信箱"，但架构 B 依赖的是运营商的**呼叫转移**（另一个网络功能）。Truecaller 正是这样做的。若也排除转移，则只剩 A（体验受限）和 C（门槛极高）。
- **目标市场**：若主打国内，小爱通话已免费内置且体验好，第三方空间小；海外市场（Call Screen 未覆盖的机型/运营商）反而是架构 B 的空间。
- **录音合法性**：各法域要求不同（部分地区需双向同意或至少明确提示）。产品上建议：录音时向对方语音提示"来电将被录音"（很多司法辖区的最低要求），App 内提供地区合规模板。此项非法律意见，上线前需按目标市场咨询。
- **技术待验证**：主流机型矩阵上"免提+MIC 录音"的实际拾音质量；`acceptRingingCall` 在 Android 14/15 各 ROM 上的可用性差异。

---

## 7. 推荐路线

1. **第 1 步（验证，2–4 周）**：做架构 A 的 MVP，验证"10 秒自动接 → 播语音 → 录音 → Whisper+LLM 摘要"的完整闭环，重点测 5–10 台主流机型的拾音质量。
2. **第 2 步（产品化分叉）**：
   - 若体验达标且目标用户是个人/极客 → 按架构 A 上架（Google Play 对通话类 App 有政策审查，需提供无障碍/通话权限的用途说明）。
   - 若要商业级体验 → 转架构 B：接入 Twilio/Vonage/_provider_ 类语音云（或国内运营商中转服务），复用第 1 步的 ASR/LLM/多语言语音管线，把"接听点"从手机搬到云端，App 变成配置与结果面板——这条路 Truecaller 已验证。
3. **长期**：若与某 OEM 建立合作，升级为架构 C 获得原生体验。

---

## 8. 追加方案：双机声学耦合架构 A2（2026-09-22）

**设计**：两台手机分工——A 机（持有 SIM、接电话）只负责自动接听；B 机（不参与通话）负责放语音、录音、转写、摘要。两机近距离摆放（最好做一个支架/耦合坞固定相对位置）。

**信号链**

```
下行（机器人语音 → 对方）：B 扬声器 ─空气─> A 麦克风 ─上行─> 对方
上行（对方语音 → 录音）：  对方 ─下行─> A 听筒 ─空气─> B 麦克风 ─> B 录音
```

A 全程**不需要开免提**（免提是单机方案的必需品，双机方案里两条方向各有一条专门的声学通道）。

**为什么绕过了限制**：B 根本不在通话里——它只是"一台在放音、一台在录音的普通手机"，用的是 `RECORD_AUDIO` + `MediaPlayer` 这类无任何限制的普通 API；A 只需要自动接听权限。**没有任何 API 撞墙点**，限制全部变成物理/工程问题。

**相比单机免提方案的实际收益**

1. **录音质量大幅提升**：B 的麦克风专录 A 的听筒（近距离高信噪比），且 B 完全知道自己正在播放什么音频，可以用软件回声消除（AEC，以自身播放为参考信号）把自己 greeting 的串音从录音里减掉 → 接近"只有对方人声"的干净录音。单机方案做不到这一点。
2. **旁听隐私改善**：对方的声音只从 A 的听筒出来（不再外放），旁人听不到通话内容；旁人能听到的只剩 B 外放的 greeting（低敏感度）。
3. **B 端完全自由**：48kHz WAV、边放边录、本地实时 ASR（Whisper）、跑本地 LLM 都可以——B 没有通话模式、没有省电限制、没有音频路由约束。A 甚至可以是一台很烂的旧手机。
4. **全程本地、零云费用**：与云端接线架构（B 路线）相比无按分钟计费。

**新设计的限制（本次问题的核心答案）**

1. **物理形态是首要代价**：两台设备必须同址、相对位置固定（不同摆放位置 = 音量/串扰漂移），实际需要一个固定支架或声学耦合坞（参考上世纪调制解调器的声学耦合器：密封声道 + 隔音棉）。产品从"一个 APK"变成"双机 App + 支架/小硬件"。
2. **B 必须留守**：放语音和录音都发生在 B 上，B 不能随身带。真实产品形态是"**双机留言坞**"：A、B 两台（可以是旧手机）常年驻留在家/办公室底座上，摘要通过推送（邮件/IM/自建通道）发到你随身的任何设备。随身方案不成立——除非接受降级。
3. **音质天花板仍在（但抬高了一层）**：对方听到的仍是"扬声器里放出来的声音"经二次电声转换（B 扬声器 → A 麦克风 → 语音编码）。且 A 的上行语音 DSP（降噪 ANS/自动增益 AGC）可能把注入的语音当噪声处理或压缩，**因机型而异**，需要机型矩阵实测调优（拉近/提高 B 的播放信噪比是主要手段）。
4. **串扰与回声工程**：B 自己的播放声会串进 B 的麦克风（软件 AEC 可解，见上）；A 的听筒漏音会进 A 的麦克风（对方听到自己轻微回声——手机通话 DSP 自带的 AEC 本来就是压听筒漏音的，通常可接受）；两条声道之间的物理隔音（泡沫/密封槽）决定整体质量上限。
5. **双机协同工程**：设备配对与发现（BLE/局域网）、"已接听"事件同步、放音/录音启停时序、以及**降级策略**——B 不在时 A 是否回退为单机免提模式，或仅静默录音。
6. **无法远程接管通话**：Call Screen 类产品可以在同一台设备上"中途自己接过来"；双机方案里对方被 A 接走后，用户无法从远处接管这通 GSM 通话。定位就是纯粹的留言机（重要电话靠对方回拨/你看到摘要后回电）。
7. **两台常开设备的供电与待机管理**（底座充电、A 保持 SIM 在网）。
8. **合规不变**：录音告知义务与法域要求与之前相同，且需要在 greeting 里保持"来电将被录音"类提示。

**升级路径（把声学耦合变数字耦合）**

- **蓝牙 HFP 角色反转**：让 B 以"蓝牙耳机"身份接入 A——B 的麦克风就是 A 的上行麦克风、A 的下行音频走蓝牙到 B。通话音质变成**全数字、零空气声**，是本设计的理想终态。但公开 Android SDK 不向普通应用开放 HFP 客户端（Hands-Free）角色（`BluetoothHeadsetClient` 属系统/嵌入场景 API），需要定制 ROM、root + 特权蓝牙权限或 OEM 配合，工程门槛高，列为 v2 研究方向。
- **有线耦合变体**：用现成的"吸盘拾音器"（贴在听筒上的老式电话录音配件，约几十元）接入 B 的 USB-C 声卡麦克风输入，解决录音侧的隔音问题；放音侧（B→A 麦克风）仍是声学。A 侧往耳机口注入的方案受机型音频路由限制（插外设常导致听筒被切换），不推荐。
- **降级阶梯**：完整双机坞 → B 缺席时 A 单机免提 → B、外放均不可用时仅拒接/静音。

**结论**：双机方案确实绕过了 API 限制（把系统权限问题转化成了声学工程问题），录音侧和旁听隐私比单机免提方案有实质提升；代价是产品形态从纯软件变成"双机 + 坞"，且对方听到的语音质量仍受声学二次转换与 A 机语音 DSP 的影响。作为完全本地、零订阅成本的方案，工程上值得先做原型验证（两台旧手机 + 泡沫支架即可开始测试）。

**Android 官方文档（关键 API 事实）**
- TelecomManager.acceptRingingCall（弃用与权限）：[developer.android.com/reference/android/telecom/TelecomManager](https://developer.android.com/reference/android/telecom/TelecomManager)
- InCallService（ROLE_DIALER、answer、setAudioRoute、响铃职责）：[developer.android.com/reference/android/telecom/InCallService](https://developer.android.com/reference/android/telecom/InCallService)
- MediaRecorder.AudioSource（VOICE_CALL 需 CAPTURE_AUDIO_OUTPUT）：[developer.android.com/reference/android/media/MediaRecorder.AudioSource](https://developer.android.com/reference/android/media/MediaRecorder.AudioSource)

**竞品**
- Truecaller Assistant（功能、Premium、Microsoft Personal Voice 声音克隆，2024-05 官宣）：[truecaller.com/assistant](https://www.truecaller.com)（官方"Set Up Your Own Voice for Truecaller Assistant"支持文章，2024-05-22）
- Truecaller 美国运营商呼叫转移激活（架构 B 的直接证据）：[support.truecaller.com — Assistant Activation in the US](https://support.truecaller.com)
- 三星 Bixby Text Call（语言与机制）：[Samsung Research 博客](https://research.samsung.com/blog/Bixby-Text-Call)、[samsung.com 使用指南](https://www.samsung.com)
- 小米小爱通话（原 AI 通话，自动接听/字幕/男女声）：[小爱同学官网](https://xiaoai.mi.com)、[小爱通话隐私政策](https://privacy.mi.com)
- YouMail / Google Voice：呼叫转移型语音信箱替代（通用常识 + 产品官网）

> 注：API 行为以官方文档为准；竞品细节以各官网/支持页为准，标注日期为核实日期。
