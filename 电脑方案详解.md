# 电脑方案详解：MacBook Air + 一加 Ace 5 至尊版（ADB 无 App 架构）（2026-09-22）

> 定位：本文是[部署方案.md](部署方案.md) 第 6 节的展开，给出可直接照做的硬件、软件、脚本与运维细节。
> 架构回顾：手机零安装；Mac 通过 USB + ADB 检测来电→10 秒自动接听→Mac 扬声器外放 greeting→Mac 麦克风录音（半双工）→本地转写→本地 LLM 摘要。

---

## 1. 硬件准备

| # | 物品 | 要求 | 说明 / 参考价 |
|---|---|---|---|
| 1 | MacBook Air | **Apple Silicon（M1 及以上），建议 ≥16GB 内存** | 无风扇设计（M 系 Air）对录音是巨大优势，自身噪音不污染录音。16GB 才能舒服跑 whisper-medium + 7B 本地 LLM；8GB 需降级到 small 模型 + 3B LLM |
| 2 | 一加 Ace 5 至尊版（A 机） | 装 SIM 卡、保持系统更新到位 | 全程零安装，保持原生 |
| 3 | **USB-C 数据线** | 必须是**数据线**（很多线是纯充电线！） | USB 2.0 速率足够 ADB；建议备 2 根（¥20–50） |
| 4 | USB 领夹麦克风（可选） | 要恢复"录对方留言"才需要 | 固定在 A 机**顶部听筒缝旁 1cm**（顶部边框，平放朝上出声）；Mac 内置麦位置不适合贴靠（¥80–300） |
| 5 | 手机支架 / 小斜垫 | 把 A 机固定在 Mac 扬声器侧 10–20cm 处，底边麦克风朝向 Mac | 亚克力/3D 打印/书本皆可；关键是一次固定后不再挪动（¥0–50） |
| 6 | 桌面垫（可选） | 泡沫/毛毡垫 | 隔离桌面敲击振动串入录音 |
| 7 | 测试用第二手机 | 任意 | 做试呼验证 |

**摆放要点**：A 机平放在 Mac 键盘一侧、**底边（主麦克风孔）朝向 Mac 扬声器格栅**，距离 10–20cm。Mac 扬声器比手机听筒响得多，所以这条通道比双机方案宽松；但位置一旦定好就不要动（greeting 音量按此位置调好）。若装了领夹麦，麦头贴 A 机顶部听筒缝，两者互不干扰（一南一北）。

**供电**：A 机插在 Mac 的 USB-C 口即可（待机耗电远低于接口输出）；如需快充或多设备（麦 + 手机），用一个**带 PD 透传的 USB-C 集线器**（约 ¥100）。

---

## 2. 软件准备

### 2.1 手机侧（一次性）

1. 开发者选项：设置 → 关于本机 → 版本信息 → 连点"版本号"7 次。
2. 打开 **USB 调试**；若 ColorOS 上 `input keyevent` 无效，再打开 **USB 调试（安全设置）**（需登录账号/验证 SIM，此开关允许 ADB 模拟输入）。
3. 首次连线时在手机上勾选"**一律允许使用这台计算机进行调试**"。
4. 开发者选项里建议开启"**充电时保持唤醒（不锁定屏幕）**"——先保证接听可靠；若验证锁屏状态下 HEADSETHOOK 接听正常，可关掉此开关以省屏幕。
5. 可选：把铃声调小或静音（`adb shell media volume --stream 2 --set 0`），改由 Mac 通知提醒来电（避免办公室里手机干响 10 秒）。

### 2.2 Mac 侧

```bash
# 基础工具
brew install --cask android-platform-tools   # adb
brew install ffmpeg scrcpy                   # 录音 / 投屏远控
brew install ollama && ollama pull qwen2.5:7b-instruct-q4_K_M   # 本地摘要 LLM（8GB 内存换 3b 模型）
pipx install mlx-whisper                     # Apple Silicon 版 Whisper（或 brew install whisper-cpp）

# 服务目录
mkdir -p ~/AICallAssistant/{greetings,recordings,transcripts,summaries,logs}
```

**系统权限**：系统设置 → 隐私与安全性 → **麦克风**，勾选将运行脚本的终端（Terminal/iTerm）；若用 ffmpeg 录音还需在"完全磁盘访问/输入监控"无要求，仅麦克风即可。

**Greeting 文件**：`greetings/zh-CN.wav`（16kHz 单声道 WAV，≤10 秒），内容必须包含"**您好，对方暂时无法接听，请在提示音后留言，本次通话将被录音**"（合规提示）。多语言各存一份（`en-US.wav`、`ja-JP.wav`…），来源三种：自己录音 / edge-tts 免费合成 / ElevenLabs 声音克隆（对应用户"AI 生成语音"需求）。**选定一套固定音量**（afplay 前把 Mac 输出音量锁定，关闭"根据内容自动调节"）。

**运行环境**：脚本用 **launchd** 设为登录自启（或先在 tmux 里手动跑通）；Mac 设置为接电源时**不系统休眠**（`caffeinate -s` 或 系统设置→电池→防止自动进入睡眠；显示器休眠没关系，系统休眠会让服务停摆）。

### 2.3 脚本骨架（`~/AICallAssistant/assistant.py`）

> 骨架用于跑通链路，解析规则和异常处理按真机输出加固。核心状态机：IDLE → RINGING（10s 计时）→ ACTIVE（放音→录音）→ 转写/摘要。

```python
#!/usr/bin/env python3
import subprocess as sp, time
from pathlib import Path

ADB = "adb"
BASE = Path.home() / "AICallAssistant"
GREETING = BASE / "greetings/zh-CN.wav"
AUTO_DELAY = 10          # 自动接听延时（秒）

def adb(*a):  return sp.run([ADB, *a], capture_output=True, text=True).stdout

def call_state():
    # mCallState: 0=idle 1=ringing 2=offhook（telephony.registry 各版本最稳定）
    for ln in adb("shell", "dumpsys", "telephony.registry").splitlines():
        if "mCallState" in ln:
            return int(ln.split("=")[1].strip().split(",")[0])
    return 0

def notify(m): sp.run(["osascript", "-e", f'display notification "{m}" with title "AI来电助手"'])

def answer(): adb("shell", "input", "keyevent", "85")   # KEYCODE_HEADSETHOOK

def main():
    notify("服务已启动，等待来电")
    ring_at = None
    while True:
        s, now = call_state(), time.time()
        if s == 1:                                        # 响铃中
            if ring_at is None:
                ring_at = now; notify("来电，10 秒后将自动接听")
            elif now - ring_at >= AUTO_DELAY:
                answer(); time.sleep(2)                   # 等接通报起
                if call_state() == 2:
                    ts = time.strftime("%Y%m%d-%H%M%S")
                    wav = BASE / "recordings" / f"{ts}.wav"
                    sp.run(["afplay", str(GREETING)])     # ① 外放 greeting（放完才开始录音→免回声消除）
                    notify("录音中…")
                    rec = sp.Popen(                        # ② 半双工录音，通话结束即停
                        ["ffmpeg", "-y", "-f", "avfoundation", "-i", ":0", str(wav)],
                        stdout=sp.DEVNULL, stderr=sp.DEVNULL)
                    while call_state() == 2: time.sleep(1)
                    rec.terminate(); notify("留言已保存，转写中…")
                    # ③ 转写+摘要（TODO 加固）：
                    #   mlx_whisper.transcribe(wav, path_or_hf_repo="mlx-community/whisper-medium-mlx")
                    #   sp.run(["ollama", "run", "qwen2.5:7b-instruct-q4_K_M", prompt])
                    #   写入 transcripts/ summaries/，osascript 通知
        else:
            ring_at = None                                # 用户自己接了/对方挂了→复位
        time.sleep(1)

if __name__ == "__main__": main()
```

注意：ffmpeg 的 `-i ":0"` 是 AVFoundation 音频设备号，先用 `ffmpeg -f avfoundation -list_devices true -i ""` 查看麦克风编号；来电号码可从 `dumpsys telephony.registry` 的 `mCallIncomingNumber` 字段取，用于日志命名与黑白名单。

---

## 3. 日常使用注意事项

**每次开机/回到工位**
- 确认脚本在跑（launchd 或 tmux）、`adb devices` 显示 device（若显示 unauthorized → 手机上重新勾选授权）。
- Mac 音量条位置没被改动（greeting 音量依赖固定输出）。

**来电时**
- 人在电脑旁：Mac 会弹通知；**10 秒内自己拿起 A 机接**即可，脚本检测到 offhook 自动取消（不需要任何操作）。
- 人不在：10 秒后自动接听，对方听到 greeting 后留言；回来后在 `recordings/ transcripts/ summaries/` 里看结果。
- 自动接通后你想立刻接手：直接拿起 A 机说话即可，但**当次 greeting 会播完**（脚本无法区分"它接的"和"你接的"）——想即时掐断可给脚本加一个全局热键杀掉 afplay 进程。

**录音质量**
- 录音期间（其实全程）Mac 开**专注模式**：任何系统提示音、邮件"叮"都会被录进去并放给对方听。
- 录音期间别敲键盘（结构噪声进麦）；Air 无风扇，无此顾虑。
- 定期（每周）用第二台手机试呼一遍全链路：响铃→接听→greeting 清晰度→留言→转写摘要。

**机器与安全**
- A 机专用化：这台手机就当"带号码的座机"，别日常揣走——**它离开底座，整套系统就没有接听端**（你带走手机=无人接听；外出一律自己接，或另行评估呼叫转移）。SIM 卡建议开通来电转移查询，作为外出的兜底选项（涉及运营商功能，是否启用由你决定）。
- USB 调试"一律允许"= Mac 获得手机完全控制权：Mac 必须设开机密码 + FileVault；手机锁屏密码照设；不用的临时电脑一律不授权。
- 手机系统大版本更新可能重置开发者选项/重新弹授权，更新后跑一次试呼验证。
- 麦克风/录音合规：greeting 中的录音提示语**不要删**；如用于办公场所，遵守当地双同意/告知要求（非法律意见）。

**数据管理**
- 目录结构按 `recordings/ summaries/` 日期滚动；16kHz 单声道 WAV 约 2MB/分钟，月度清理或转 m4a 归档。
- 转写失败的通话（Whisper 偶发失败）保留原始录音人工听，不要静默丢弃。
- 摘要推送（可选进阶）：脚本末尾加一条 osascript 通知/邮件；若你有手表/第二设备，可经邮件中转。

**故障速查**

| 症状 | 排查 |
|---|---|
| `adb devices` 无设备 | 换数据线（最常见！纯充电线）、换 USB 口、重新插拔触发授权弹窗 |
| 响铃但没自动接 | 看 `dumpsys telephony.registry` 的 mCallState 是否真是 1；`input keyevent 85` 手动执行验证（无效则开"USB 调试（安全设置）"） |
| 对方听 greeting 太小声 | Mac 输出音量提高、手机挪近扬声器、确认手机没被重新摆放 |
| 录音全静音 | 系统设置→隐私→麦克风→勾选终端；ffmpeg 设备号是否选对 |
| Mac 睡眠后漏接 | caffeinate -s / 电源设置防休眠；launchd 是否加载 |

---

## 4. 与其他形态的关系

| | 双机方案 | **本文：Mac+ADB** | 云端接线 |
|---|---|---|---|
| 手机侧改造 | A 装 App、B 装 App | **零安装** | 呼叫转移设置 |
| 音频通道 | 专用支架贴靠 | Mac 扬声器/麦克风（更宽松） | 云端，无物理耦合 |
| 智能 | B 机本地 | Mac 本地（最强） | 云侧 |
| 限制 | 人走带不走 B | **手机离开底座即失效** | 费用/转移依赖 |

---

## 5. 录对方留言（2026-09-22 追加实现）

代码已支持（`code/assistant.py`）：接通后自动连按"音量上"拉满通话音量（`call_volume_keys` 配置），对方声音从**顶部听筒缝**输出（手机保持听筒模式），由 `ffmpeg_audio_device` 指定的录音设备拾取。录音仍是半双工时序（greeting 播完才开始），无回声消除负担；greeting 内已含"本次通话将被录音"告知。

三种拾音摆法（详细步骤见 [code/README.md 第 3.5 节](code/README.md)）：

1. **零硬件**：手机横放在 Mac 左掌托前缘——底边（麦克风）朝左侧扬声器格栅、顶边（听筒）朝键盘左上的麦克风开孔区，用 `python3 assistant.py --record-test 6` 反复标定位置（对着听筒弹指，max 音量 ≥ -20dB 为合格）；
2. **USB 领夹麦（推荐）**：手机留原位不动，麦头贴听筒缝旁 5–10mm，`ffmpeg_audio_device` 改为新设备号；
3. **USB-C 转接头 + 耳麦（最稳）**：耳机麦贴 Mac 扬声器格栅、耳塞贴 Mac 麦克风孔，全数字路径；转接头需带 PD 透传，否则手机无法边用边充电。

摆放变化后必须重跑一次 `--record-test` 标定，合格再固定位置。
