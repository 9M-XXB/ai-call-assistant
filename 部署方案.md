# 双机部署方案：一加 Ace 5 至尊版（接听）+ Nothing Phone 4 Pro（录音/放音）（2026-09-22）

## 1. 两台机器的音频开孔分布

**A 机：一加 Ace 5 至尊版**（官方规格：超线性立体声双扬声器 + 3 麦克风；据官方快速指南与充电头网评测）

| 位置 | 开孔 |
|---|---|
| **顶部边框** | 听筒（隐藏式微缝扬声器，平放时朝上出声）、副麦克风、红外开孔 |
| **底部边框** | 主扬声器（USB-C 左侧）、主麦克风（USB-C 右侧）、USB-C 居中、SIM 卡槽 |

**B 机：Nothing Phone 4 Pro**（据 iFixit/Nothing 系列拆解，同系布局一致）

| 位置 | 开孔 |
|---|---|
| **底部边框** | 主麦克风（USB-C 左侧针孔）、主扬声器（USB-C 右侧格栅）、USB-C 居中、SIM |
| **顶部** | 听筒扬声器（顶部扬声器模组）、副麦克风（具体开缝位置见第 4 节校准） |

> 关键利好：A 机的听筒在**顶部边框**上（平放时向上出声），且与 A 的副麦克风相邻——这让"录音通道"可以做到 1–2cm 的贴靠耦合。

## 2. 摆放方案（两台都不用免提模式）

**角色分配**：A 机保持**听筒模式**接听（下行从顶部听筒缝出声）；B 机用**顶部听筒扬声器**放 greeting（不用主扬声器），用**底部主麦克风**录音。

**布局：一字排开、头对脚、B 上移压住 A 的顶部**（俯视图）：

```
 桌面俯视：                     北
        ┌─────────────┐
        │  B (Nothing) │  屏幕朝上，纵向倒置（头朝南）
        │  底边↑朝A    │  ← 底边：主麦克风+主扬声器(静默)
        └──┬────────┘
      ~8mm │ 泡沫垫圈/支架高度差
        ┌──┴─────────┐
        │ A (一加)    │  屏幕朝上，纵向正放（头朝北）
        │ 顶边：听筒缝 │  ← 录音通道：A听筒 ─1cm→ B主麦克风
        │            │
        │            │
        └──△─────────┘
           △ = A 底边主麦克风
           ↑ 放音通道：B顶部听筒扬声器 ─~18cm→ A主麦克风（加泡沫导音管）
```

**两条声学通道**：

| 通道 | 路径 | 耦合要求 |
|---|---|---|
| 录音（对方→B） | A 顶部听筒缝（朝上出声）→ B 底部主麦克风，**间距 5–15mm** | 质量关键，贴靠 + 泡沫垫圈密封，B 端软件 AEC 消掉自己 18cm 外的 greeting 残留 |
| 放音（B→对方） | B 顶部听筒扬声器（朝上出缝）→ A 底部主麦克风，**间距 ~18cm** | 宽松通道：音量可编程拉高；泡沫导音管（直径 10–15mm、长 3–5cm 的保温管/纸筒）对准 A 的麦克风孔，抑制串到 A 顶部副麦克风 |

**两个必须做的物理细节**：

1. **横向错位 2–3cm（错位狗腿）**：A 有 3 个麦克风，波束成形/降噪会把"到达两麦强度对称的声音"当噪声压掉。B 相对 A 横向错开后，greeting 到 A 主麦与副麦的距离/角度不对称，会被当作正常近端人声放行；同时错位对录音通道影响很小（仍 <3cm）。
2. **B 的主扬声器全程静默**：它的开孔与 B 主麦克风同在底边、且紧邻 A 的听筒与副麦——一旦出声会同时污染录音通道和 A 的降噪参考。greeting 只从 B 顶部听筒扬声器出。

**若实测 B 听筒扬声器音量不够**（被 A 的降噪压制或穿透力不足），降级阶梯：
1. 泡沫导音管加长/对准，再拉高 B 媒体音量；
2. 改用 B 主扬声器 + 长导音管直插 A 底部麦克风孔（仍不是"免提模式"，但 B 主扬声器出声需把 B 的 AEC 参考做严）；
3. 最后才接受 A 开免提（回到单机式布局，B 主麦克风对 A 扬声器）。

### 2.5 变体：去除录音通道的"贴靠布局"（2026-09-22 补充）

若不需要 B 录对方留言，两机可以**直接贴靠**，且布局应反转——B 掉头，让 B 的**顶部听筒端贴住 A 的底部麦克风端**：

```
      ┌────────────┐
      │ A (一加)    │  底边主麦 ← greeting（本方案唯一声学通道）
      └──△─────────┘
      ┃ 1-2cm 直接贴靠（B 远端垫 5–10° 斜楔，听筒缝对准 A 麦孔）
      ┌──╨─────────┐
      │ B (Nothing) │  顶边听筒缝出 greeting
      └────────────┘
```

优点：greeting 距离从 ~18cm 缩到 1–2cm，强近场源特征与"真人凑近说话"一致，A 的多麦降噪会正常放行（不再需要横向错位与导音管）。物理接触检查：平铺贴靠优于叠放（避开双方 camera island 与侧键）；充电走 USB 线；响铃振动传导无害；射频/磁体无影响。

**代价必须知悉**：录音通道是对方留言的唯一拾取路径。去掉后 A 录不了（`VOICE_CALL` 需系统权限；听筒模式下对方声音几乎不漏进麦克风，部分机型通话中直接禁用第三方麦克风），B 掉头后其麦克风距 A 听筒 16cm 也录不清——产品退化为"接听+greeting+挂断"，**摘要功能失去原料**。若仍需留言+摘要：回到第 2 节原布局，或接受 A 开免提由 B 任意位置拾音。

## 3. 双机同步方案

**校园网的关键问题**：校园 Wi-Fi 几乎必然开启 AP 隔离（客户端互不可达），A、B 同连校园网**不能**用于两机直连。你提出的"Nothing 开热点、一加接入"可以解决直连问题，但有两个代价：

- **热点共享的是 B 的移动数据**，不是校园网——热点期间 B 的云侧调用（ASR/LLM）走流量；
- A 接入热点后自身没有互联网（A 不需要，但要注意它的其他 App 失联）；热点待机功耗高。

**推荐架构：运行期用 BLE，热点只做备选**

```
运行期（通话时）：BLE GATT 控制通道
  B = GATT 服务端（大脑），A = 客户端（无状态的话务接口）
  - BLE 不受 AP 隔离影响、不耗数据、待机功耗低、断线自动重连
  - 两台手机各自可继续连校园网上网（互不影响）

大文件/调试期：B 开热点 + A 接入，TCP/WebSocket + mDNS 发现
  仅用于初次导入 greeting 音频、固件级配置、抓日志
```

**控制协议（BLE GATT，自定义 Service，JSON over 特征值）**：

| 方向 | 事件/命令 | 载荷 |
|---|---|---|
| A→B | `INCOMING` | 来电号码、时间戳 |
| A→B | `ANSWERED` | 接听时刻（A 的 10s 定时器触发 `call.answer()` 后立即发） |
| A→B | `CANCELLED` | 用户在 10s 内手动接听/拒接 |
| A→B | `ENDED` | 通话时长、结束原因（对方挂断/超时上限） |
| B→A | `POLICY` | 是否允许自动接听（B 离线时 A 默认不接）、最长相留言时长 |
| B→A | `HEARTBEAT` | 30s 心跳，A 超时未收到则视 B 离线 |

**时序预算**：A 接听 → BLE 通知（<100ms）→ B 起播（缓冲 50–150ms）→ 对方听到 greeting ≈ 接听后 300–500ms。这个"喂？……您好"的自然停顿反而是拟真的优点。时间同步只需事件顺序一致（B 为主时钟，事件带单调时戳），无需 NTP 级对齐。

**一个重要的架构推论**：A 机是完全无状态的——所有 greeting 音频、录音、转写、摘要都在 B 上。因此 A 的 App 极小（自动接听 + 事件上报），音频文件根本不需要在两机间传输，热点在运行期并非必需。

## 4. 落地前的校准与验证（一次性）

1. **开孔确认**：两机的听筒缝/麦克风实际位置以实测为准（Nothing 顶部听筒是前面板顶缝还是中框顶缝未确认）：B 进 App 的"麦克风标定"模式，逐个用嘴吹/放纯音靠近每个孔，App 用 `AudioRecord.setPreferredDevice()` 枚举内置麦并锁定"底边主麦"。
2. **摆位标定**：B 持续录音并显示实时电平，用户把 B 移到 A 顶部听筒附近找电平峰值位置，App 记录并提示支架卡位。
3. **试呼**：用第三台手机拨打 A，走完整流程（10s → 接听 → greeting → 留言 → 挂断），检查：对方听 greeting 的清晰度、A 是否把 greeting 当噪声压制、B 录音里 greeting 残留是否被 AEC 消干净。
4. **支架**：3D 打印/木托盘开两个手机凹槽（总占地约 35×10cm）+ 泡沫垫 + 导音管 + 常供电 USB 线。

## 5. 工程 checklist（两台都要做）

- A：`ANSWER_PHONE_CALLS` 运行时权限（或 ROLE_DIALER + `InCallService`，自管铃声可在接听瞬间停振）；禁电池优化；前台服务。
- B：`RECORD_AUDIO` + 前台服务（录音类型）+ `WAKE_LOCK`；禁电池优化；**录音期间开 DND**（防振动通知污染录音）；媒体音量程序化锁定。
- 双机：BLE 配对用首次扫码绑定，之后自动重连；日志含事件时戳序列，便于排查串扰/延迟问题。

## 6. 变体：用电脑（MacBook Air）替代 B 机（2026-09-22 补充）

**结论：连接/投屏不改变音频墙，但电脑可以取代 B 机，且能做到"手机零安装"。**

### 6.1 O+ Connect 能做什么、不能做什么

| 能力 | 可用性 | 说明 |
|---|---|---|
| 投屏（手机→Mac） | ✅ | Mac App Store 有 O+ Connect，支持文件传输、剪贴板、投屏 |
| Mac 端远控手机 UI | ✅ | 可在 Mac 上点接听/挂断（人工或脚本点坐标，后者脆弱） |
| 通话音频上屏（对方声音→Mac） | ❌ | 投屏通道至多转发媒体音频；通话流受 `CAPTURE_AUDIO_OUTPUT` 保护，O+ Connect 作为普通 App 同样抓不到 |
| Mac 音频注入通话 | ❌ | Mac 声音到手机只是媒体播放，进不了通话上行——墙壁在手机 OS 内部，与连不连电脑无关 |

O+ Connect 的正确定位：**监控窗口 + 人工接管 + 传文件**，不是自动化或音频通道。

### 6.2 更彻底的路径：ADB 无 App 方案（推荐）

Mac 通过 USB 线连 A 机，手机**不需要装任何 App**，逻辑全部在 Mac 端脚本：

```
1. 来电检测：轮询 `adb shell dumpsys telecom`（约 1s 粒度，足够 10s 定时器）
2. 等待 10s 无人接 → `adb shell input keyevent KEYCODE_HEADSETHOOK` 接听
3. Mac 扬声器外放 greeting（对准 A 底部麦克风，校准方法同第 2 节）
4. greeting 播完 → 停止播放 → Mac 麦克风开始录音
   （半双工时序天然免去回声消除——播放与录音不重叠）
5. 挂断检测（dumpsys）→ Whisper（Apple Silicon 本地）→ 本地 LLM 摘要
```

运维注意：USB 调试需"一律允许此计算机"（注意安全含义：adb 权限等于完全控制手机）；`KEYCODE_HEADSETHOOK` 接听在锁屏态通常有效，需真机验证；手机常插 USB（本来就驻留底座）；全程 USB 连接，**校园网 AP 隔离不再相关**。

### 6.3 网络与工具选择

- 校园网 AP 隔离同样会影响 O+ Connect（走同一局域网）——若必须无线投屏，用手机开热点或 Mac 开互联网共享建立私有网；**更稳的做法是 scrcpy over USB**（开源，投屏+远控+免网络，macOS 原生支持），可作为 O+ Connect 的工程级替代。
- 若要恢复"录对方留言"：Mac 内置麦克风位置不适合贴靠 A 的听筒，建议加一支 USB 领夹麦（接 Mac）固定在 A 听筒缝旁 1cm，录音质量即回到第 2 节水平；Mac 端软件 AEC 同样可用（greeting 与录音仍可按时序错开）。

### 6.4 三种"大脑"形态对比

| 形态 | 手机侧 | 音频耦合 | 智能（ASR/LLM） | 适用 |
|---|---|---|---|---|
| 双机（第 2 节） | A 装小 App | 专用支架，最紧凑 | B 机本地 | 无电脑场景 |
| **电脑 + O+ Connect/scrcpy + ADB** | **零安装** | Mac 扬声器/麦克风 + 校准 | Mac 本地，最强 | 桌面常驻（宿舍/办公室） |
| 云端接线（可行性分析 第 3 节 B） | 呼叫转移 | 无（云端处理） | 云侧 | 商用级、跨设备 |


- 一加 Ace 5 至尊版规格（超线性立体声双扬声器、3 麦克风）：[一加官网规格页](https://www.oneplus.com)；顶部听筒/麦克风/红外、底部扬声器与麦克风开孔：充电头网《一加 Ace 5 至尊版深度评测》（chongdiantou.com）及官方快速指南开孔图。
- Nothing 系列底部布局（USB-C 居中、左侧针孔麦、右侧扬声器格栅）与顶部扬声器模组：iFixit Nothing 维修指南与 Nothing Community 拆解帖（nothing.community）。Note：搜索主要命中 Phone (4a) Pro，Phone (4) Pro 顶部听筒开缝的确切位置按第 4 节标定流程实测确认。
